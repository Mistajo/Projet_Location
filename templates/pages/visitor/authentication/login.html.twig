<!DOCTYPE html>
<html>
	<head>
		{% block SEO %}
			<meta charset="UTF-8">
			<meta http-equiv="X-UA-Compatible" content="ie=edge">
			<meta name="viewport" content="width=device-width, initial-scale=1.0">
			<meta http-equiv="X-UA-Compatible" content="IE=7">
		{% endblock %}


		<title>Ride-Location : Connexion
		</title>

		{% block description %}
			<meta name="description" content="Bienvenue sur notre page de connexion. La société Ride-Location vous propose des locations de voitures en click en collect. Cette sociétéé est détenu pas Monsieur Dwayne Johnson. Nous avons plusieurs agences en France.Venez faire un tour sur notre site, on a ce qu'il vous faut.">
		{% endblock %}


		{# Favicon #}
		<link rel="apple-touch-icon" sizes="180x180" href="{{asset('favicon/apple-touch-icon.png')}}">
		<link rel="icon" type="image/png" sizes="32x32" href="{{asset('favicon/favicon-32x32.png')}}">
		<link rel="icon" type="image/png" sizes="16x16" href="{{asset('/favicon-16x16.png')}}">
		<link rel="manifest" href="/site.webmanifest">
		<meta name="msapplication-TileColor" content="#ffffff">
		<meta
		name="theme-color" content="#ffffff">

		{# Font family #}
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Caveat&family=Pacifico&display=swap" rel="stylesheet">


		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}

		{% block javascripts %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}
	</head>
	<body class="h-100">

		<div class="container mt-5">
			<div class="row">
				<div class="col-lg-6 d-flex justify-content-center align-items-center">
					<img src="{{asset('images/visitor/authentication/Connectez-vous-pour-louer-des-vehicules.png')}}" class="img-fluid" alt="Connectez vous pour louer des vehicules">

				</div>
				<div class="col-lg-6 d-flex flex-column justify-content-center align-items-center">
					<form method="POST" class="w-100">
						{% if error %}
							<div class="text-center alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
						{% endif %}

						{% for message in app.flashes('success') %}
							<div class="alert alert-success text-center" role="alert">
								{{message}}
							</div>
						{% endfor %}

						<div class="mb-3">
							<label for="authentication_form_email">Email</label>
							<input type="email" value="{{ last_username }}" name="email" id="authentication_form_email" class="form-control" autocomplete="email" required autofocus>
						</div>
						<div class="mb-3">
							<label for="authentication_form_password">Mot de passe</label>
							<input type="password" name="password" id="authentication_form_password" class="form-control" autocomplete="current-password" required>
						</div>
						<div class="row my-3">
							<div class="col-md-6 my-1">
								<label>
									<input type="checkbox" name="_remember_me" checked>
									Se souvenir de moi
								</label>
							</div>
							<div class="col-md-6 text-start text-md-end my-1">
								<a href="{{path('visitor.authentication.forgot_password_request')}}">Mot de passe oublié ?</a>
							</div>
						</div>
						<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
						<div class="mb-3">
							<input formnovalidate type="submit" class="btn btn-primary w-100 shadow" value="Je me connecte">
						</div>
					</form>

					<p class="my-1">Vous n'êtes pas encore inscrit ?
						<a href="{{path('visitor.registration.register')}}">Inscrivez-vous</a>
					</p>
					<p class="my-1">
						<a href="{{path('visitor.welcome.index')}}">Retour à l'accueil</a>
					</p>
				</div>
			</div>
		</div>
	</body>
</html>
